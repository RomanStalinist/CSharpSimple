namespace lw
{
    public static class Constants
    {
        public static readonly string CS_EOL = "\n";
        public enum SortCase { SORT_REGULAR, SORT_NUMERIC, SORT_STRING, SORT_LOCALE_STRING, SORT_NATURAL, SORT_FLAG_CASE }
        public enum CaseType { CASE_LOWER, CASE_UPPER, FIRST_UPPER, LAST_UPPER, UNDERSCORE }
    }
}
